You are merging parallel code-review outputs for `$review_scope`.

Compare the specialist results as candidate review packets.

Do the following:

- Deduplicate overlapping findings.
- Preserve the strongest evidence when multiple reviewers point to the same issue.
- Call out consensus patterns across rubrics.
- Drop low-signal repetition and stylistic nitpicks.
- Surface branch disagreements only when they affect actionability.

Return a merged findings draft that is short, evidence-grounded, and ready for one lead reviewer to polish.
