Convert the accepted review packet for `$review_scope` into a fix-request handoff for the implementer.

Produce:

1. A short summary of why fixes are required.
2. An ordered fix list.
3. The narrowest validation or regression checks to run after each fix.
4. Any issue that still requires human judgment rather than more AI iteration.
